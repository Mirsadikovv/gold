name: Deploy skoopka.uz

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - run: |
          docker compose -f skoopka/docker-compose.yml down --remove-orphans || true
          docker compose -f skoopka/docker-compose.yml up --build -d
