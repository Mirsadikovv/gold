name: Deploy e-skupka.uz

on:
  push:
    branches: [main]
    paths:
      - 'eskupka/**'

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v5
      - run: docker compose -f eskupka/docker-compose.yml up --build -d
